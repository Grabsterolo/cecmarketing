# Supabase (dashboard cliente)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Secreto compartido con /api/chat y /api/cleanup-scan (ver functions/api/)
VITE_SOFIA_SECRET=

# Base URL para abrir un prospecto directo en Zenvia Conversion desde
# Leads Potenciales (se concatena con el prospect_id al final).
# pendiente: confirmar formato exacto de URL de Zenvia Conversion
VITE_ZENVIA_WEB_BASE_URL=

# Supabase (server-side, funciones de Cloudflare Pages)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
